import React from 'react';
import PokedexContext from '../contexts/pokedex';

function MainContainer(props) {
  return (
  <div className={`info-wrapper ${props.position}`}>
  </div>
  );
}

export default MainContainer;
